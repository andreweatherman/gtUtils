% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_group_stripes.R
\name{gt_group_stripes}
\alias{gt_group_stripes}
\title{Band alternate row groups of a \code{gt} table}
\usage{
gt_group_stripes(gt_object, color = "#F5F5F5", start = 2, include_stub = TRUE)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify. It must have row groups.}

\item{color}{Character. A hex color for the banded groups. Defaults to
\code{"#F5F5F5"}.}

\item{start}{Integer. Which group to begin banding on, in the order the groups
are rendered. \code{2} leaves the first group unshaded, \code{1} shades it. Defaults to
\code{2}.}

\item{include_stub}{Logical. Should the stub column be banded along with the
body? Defaults to \code{TRUE}.}
}
\value{
Returns a modified \code{gt} table with alternate groups banded.
}
\description{
Shades every other row group, so each group reads as a block. \code{gt::opt_row_striping()}
bands every other row, which fights the grouping on a table that is already
divided into sections.
}
\details{
Groups are banded in the order they render, so this follows
\code{gt::row_group_order()} rather than the order the groups happen to appear in
the data.

The fill is applied to body cells rather than through CSS, because a group
heading occupies a row of its own and shifts every \code{nth-child} count below it.
Group heading rows are left alone; \code{gt::tab_options(row_group.background.color)}
sets those, and it sets all of them at once.
}
\examples{
\dontrun{
library(gt)

cars <- mtcars[c("mpg", "hp", "wt")]
cars$cyl <- paste(mtcars$cyl, "cylinders")

gt(head(cars, 15), groupname_col = "cyl") \%>\%
  gt_theme_broadsheet() \%>\%
  gt_group_stripes()

# shade from the first group instead, in a warmer tone
gt(head(cars, 15), groupname_col = "cyl") \%>\%
  gt_group_stripes(color = "#FBF3E4", start = 1)
}

}
