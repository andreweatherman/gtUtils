% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_drench.R
\name{gt_theme_drench}
\alias{gt_theme_drench}
\title{Saturated single-color theme for \code{gt} tables}
\usage{
gt_theme_drench(
  gt_object,
  color = "#123F5E",
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{color}{Character. The hex color the table is drenched in. Anything from a
near-black to a mid-saturation brand color works, and very pale colors flip
the type to dark automatically. Defaults to \code{"#123F5E"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
Colors the entire table surface instead of putting a colored detail on a
neutral background, tying the table to a single brand or publication color.
}
\details{
Text, rules, and secondary type are all derived from \code{color}. The type color is
whichever of black or white measures higher contrast against the background,
and the rules are built by shifting the background itself, since gray laid over
a saturated ground looks washed out.
}
\section{Legibility}{


The secondary color used for column labels, the subtitle and source notes is
blended toward the type color until it clears a 4.5:1 contrast ratio against
the background, so it stays readable whatever \code{color} you pass.
}

\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_drench.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars)) \%>\% gt_theme_drench()

# a brand color, with a matching export canvas
gt(head(mtcars)) \%>\%
  gt_theme_drench(color = "#4B1E78", density = "social") \%>\%
  gt_social_crop(bg = "#4B1E78")
}

}
\seealso{
\code{\link[=gt_theme_midnight]{gt_theme_midnight()}} for a restrained dark background instead.
}
