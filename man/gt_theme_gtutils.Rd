% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_gtutils.R
\name{gt_theme_gtutils}
\alias{gt_theme_gtutils}
\title{gtUtils theme for \code{gt} tables}
\usage{
gt_theme_gtutils(
  gt_object,
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
The package's house theme. A warm cream background, an Almarai body,
Signika Negative titles, column labels and row groups, hairline separators
between rows, and gray row-group bands carrying cream labels. All columns are
centered.
}
\details{
The row separators are a single bottom border in gray (\verb{#8A817C}) applied to
every body row except the last, so the table closes on the background rather
than a rule. The background is a warm cream (\verb{#FFFDF5}), and horizontal rules
are otherwise set transparent. A table id is resolved (or generated) up front
so the \code{gt::opt_css()} block can pin the closing row border and heading
padding to this table alone.
\code{density} rescales the finished table, since this theme sets its sizes
directly rather than deriving them from a scale.
}
\section{Density}{


\code{density} scales the theme's type and row padding together. \code{"comfortable"}
leaves every size as the theme sets it, \code{"compact"} scales both down, and
\code{"social"} scales both up, to the scale \code{\link[=gt_save_crop]{gt_save_crop()}} and
\code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_gtutils.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars)) \%>\% gt_theme_gtutils()
gt(head(mtcars)) \%>\% gt_theme_gtutils(density = "compact")
}

}
