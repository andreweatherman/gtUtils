% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_centered_legend.R
\name{gt_centered_legend}
\alias{gt_centered_legend}
\title{Add a centered title, subtitle, and legend to a \code{gt} table with customizable
label placement}
\usage{
gt_centered_legend(
  gt_table,
  key_info,
  heading = "Centered Title",
  subtitle = "Centered Subtitle",
  label_placement = "outside"
)
}
\arguments{
\item{gt_table}{A \code{gt} table object to modify.}

\item{key_info}{A tibble containing two columns: \code{color} (hex color codes)
and \code{label} (the corresponding text labels).}

\item{heading}{Character. The main title text for the table. Defaults to
\code{"Centered Title"}.}

\item{subtitle}{Character. The subtitle text for the table. Defaults to
\code{"Centered Subtitle"}.}

\item{label_placement}{Character. Either \code{"inside"} or \code{"outside"},
specifying whether the labels should be inside or outside the color boxes.
Defaults to \code{"outside"}.}
}
\value{
Returns a modified \code{gt} table with a centered title, subtitle, and a
key.
}
\description{
This function adds a centered title, subtitle, and a key or legend at the top
of a \code{gt} table. The key/legend is passed through as a tibble containing
colors and labels. Labels can be placed either inside or outside the color
boxes.
}
\details{
The function creates a centered title, subtitle, and a legend based
on the provided key information (colors and labels). If \code{label_placement} is
\code{"inside"}, the labels will be placed inside the colored boxes; if
\code{"outside"}, they will be placed next to the colored boxes.
}
