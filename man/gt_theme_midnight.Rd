% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_midnight.R
\name{gt_theme_midnight}
\alias{gt_theme_midnight}
\title{Dark theme for \code{gt} tables}
\usage{
gt_theme_midnight(
  gt_object,
  accent = "#5B8DEF",
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the row-group labels and the rule
above the table. Defaults to \code{"#5B8DEF"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
A restrained dark theme. The background is a near-black instead of pure black,
which avoids the smearing pure black causes on OLED screens. Column labels sit
on a slightly raised band, rules are hairlines, and the body has no fills.
}
\details{
Light text on a dark background reads as lighter weight than it is, so the
theme opens up the line height to compensate.
}
\section{Color scales on dark grounds}{


A green-to-red ramp built for white paper goes muddy on a near-black
background, as its mid-tones collapse toward the background. Use \link{pal_midnight}
with \code{\link[=gt_color_ranks]{gt_color_ranks()}} instead.
}

\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_midnight.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars)) \%>\% gt_theme_midnight()

# color scales need lifting on a dark ground; see pal_midnight
gt(head(airquality, 10)) \%>\%
  gt_theme_midnight() \%>\%
  gt_color_ranks(Temp, palette = pal_midnight)
}

}
\seealso{
\link{pal_midnight} for a color scale that survives a dark ground, and
\code{\link[=gt_theme_terminal]{gt_theme_terminal()}} for a denser dark look.
}
