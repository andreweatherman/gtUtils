% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_social_tag.R
\name{gt_social_tag}
\alias{gt_social_tag}
\title{Add social handles to a \code{gt} table}
\usage{
gt_social_tag(
  gt_object,
  accounts,
  caption = NULL,
  stack = FALSE,
  separator = " | ",
  align = "right",
  icon_color = NULL,
  icon_height = "0.9em",
  text_size = NULL,
  text_weight = NULL,
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accounts}{A named character vector mapping platform to handle, such as
\code{c(x = "@you", bluesky = "you.bsky.social")}. Names are matched to Font
Awesome brand icons, and friendly aliases are accepted, including \code{x} and
\code{twitter}, \code{ig} and \code{instagram}, \code{bsky} and \code{bluesky}, \code{gh} and \code{github},
\code{yt} and \code{youtube}, \code{web} and \code{website}, and \code{email}. An unrecognized name is
used as the icon name directly.}

\item{caption}{Optional. A caption rendered as the top footnote line. Defaults
to \code{NULL}, which shows only the handle line.}

\item{stack}{Logical. Should the accounts be stacked vertically, one per line,
instead of sitting in a row? Defaults to \code{FALSE}.}

\item{separator}{Character. The string placed between accounts when not
stacked. Defaults to \code{" | "}.}

\item{align}{Character. The alignment of the handle line. One of \code{"right"},
\code{"center"}, or \code{"left"}. Defaults to \code{"right"}.}

\item{icon_color}{Optional. A hex color for the icons. If \code{NULL}, they inherit
the surrounding text color. Defaults to \code{NULL}.}

\item{icon_height}{Character. The height of the icons, as a CSS size. Given in
\code{em} it scales with \code{text_size}. Defaults to \code{"0.9em"}.}

\item{text_size}{Optional. The font size for the handles, as a CSS size. If
\code{NULL}, it inherits the source-note size. Defaults to \code{NULL}.}

\item{text_weight}{Optional. The font weight for the handles. If \code{NULL}, it
inherits the source-note weight. Defaults to \code{NULL}.}

\item{...}{Additional arguments passed to \code{\link[=gt_538_caption]{gt_538_caption()}}, used only when a
\code{caption} is supplied.}
}
\value{
Returns a modified \code{gt} table with the handles, and any caption, added.
}
\description{
Renders a set of accounts, each an icon plus a handle, into the table's source
note, styled like the bottom line of \code{\link[=gt_538_caption]{gt_538_caption()}}. Icons come from Font
Awesome through the \code{fontawesome} package.
}
\details{
If a \code{caption} is supplied it becomes the top line, a bordered footnote in the
same manner as \code{\link[=gt_538_caption]{gt_538_caption()}}, with the handles beneath it. With no
caption, only the handle line is shown.

If an icon name is not present in your installed version of \code{fontawesome}, the
function stops with a message naming the icon and the version, instead of
rendering a broken glyph.
}
\examples{
\dontrun{
library(gt)

gt(head(mtcars)) \%>\%
  gt_social_tag(c(x = "@yourhandle", gh = "yourname"))

# with a caption line above, stacked and left-aligned
gt(head(mtcars)) \%>\%
  gt_social_tag(
    c(x = "@yourhandle", web = "example.com"),
    caption = "Data: R built-in datasets",
    stack = TRUE, align = "left"
  )
}

}
\seealso{
\code{\link[=gt_538_caption]{gt_538_caption()}}.
}
