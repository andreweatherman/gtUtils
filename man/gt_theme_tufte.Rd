% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_tufte.R
\name{gt_theme_tufte}
\alias{gt_theme_tufte}
\title{Minimal-ink theme for \code{gt} tables}
\usage{
gt_theme_tufte(
  gt_object,
  accent = "#111111",
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the header hairline and the row-group
labels. Defaults to \code{"#111111"}, near-black. A muted red or rust gives the
Tufte marginal accent.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
A spare, Tufte-inspired look. An old-style serif on a warm white ground,
italic column labels, and a single hairline under them. There are no vertical
rules, no rules between rows, and no fills. A faint hairline closes the body.
}
\details{
\code{accent} recolors the header hairline and the row-group labels.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_tufte.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)

gt(head(mtcars[c("mpg", "hp", "wt")], 8)) \%>\% gt_theme_tufte()

# a muted rust accent on the hairline
gt(head(airquality, 8)) \%>\% gt_theme_tufte(accent = "#7B3F2B")
}

}
\seealso{
\code{\link[=gt_theme_booktabs]{gt_theme_booktabs()}} for a firmer, academic relative.
}
