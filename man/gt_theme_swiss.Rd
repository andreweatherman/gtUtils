% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_swiss.R
\name{gt_theme_swiss}
\alias{gt_theme_swiss}
\title{International Typographic Style theme for \code{gt} tables}
\usage{
gt_theme_swiss(
  gt_object,
  accent = "#111111",
  density = c("comfortable", "social", "compact"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color, used once, on the rule beneath the
column labels. Defaults to \code{"#111111"}, which reads as no color at all.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
Builds the table on whitespace. There are no fills, no banding, and exactly
two rules in the whole table, one under the column labels and one closing the
body. Everything else is done with space and alignment.
}
\details{
Alignment is strict. Text sits flush left, numbers flush right, and nothing is
centered.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_swiss.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars)) \%>\% gt_theme_swiss()
gt(head(iris)) \%>\% gt_theme_swiss(accent = "#D33A2C")
}

}
