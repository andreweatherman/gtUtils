% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_significance.R
\name{gt_significance}
\alias{gt_significance}
\title{Add significance stars to a \code{gt} table}
\usage{
gt_significance(
  gt_object,
  columns,
  p_columns,
  levels = c(0.01, 0.05, 0.1),
  symbols = c("***", "**", "*"),
  superscript = TRUE,
  size = "0.7em",
  legend = TRUE,
  legend_text = NULL,
  hide_p = TRUE
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The estimate column or columns to annotate.}

\item{p_columns}{The column or columns holding the p-values, paired
positionally with \code{columns}. Supply one p-value column per estimate column.}

\item{levels}{A numeric vector of significance thresholds, in ascending order
(strictest first). Defaults to \code{c(0.01, 0.05, 0.1)}.}

\item{symbols}{A character vector of notation for each level. Must be the same
length as \code{levels}. Defaults to \code{c("***", "**", "*")}.}

\item{superscript}{Logical. Should the stars be rendered as superscript?
Defaults to \code{TRUE}.}

\item{size}{Character. The size of the stars, as a CSS size. Defaults to \code{"0.7em"}.}

\item{legend}{Logical. Should the legend be added as a source note? Defaults
to \code{TRUE}.}

\item{legend_text}{Optional. Custom legend text. If \code{NULL}, the legend is built
from \code{levels} and \code{symbols}. Defaults to \code{NULL}.}

\item{hide_p}{Logical. Should the p-value columns be hidden once the stars are
applied? Defaults to \code{TRUE}.}
}
\value{
Returns a modified \code{gt} table with significance notation applied.
}
\description{
Appends significance notation to an estimate column based on a column of
p-values, then writes the matching legend into a source note. The p-value
column is hidden by default, since the stars stand in for it. This is the usual
convention for regression tables in the social and medical sciences.
}
\details{
The legend is generated from the same \code{levels} and \code{symbols} used to place the
stars, so changing one updates the other.

Each value takes the notation for the strictest threshold it satisfies, so with
the defaults a p-value of 0.004 gets \verb{***} and not \code{*}. Values that meet none
of the thresholds are left alone, as are \code{NA} p-values.
}
\examples{
\dontrun{
library(gt)

fit <- lm(mpg ~ wt + hp + factor(cyl), data = mtcars)
results <- data.frame(
  Term = rownames(summary(fit)$coefficients),
  Estimate = summary(fit)$coefficients[, 1],
  SE = summary(fit)$coefficients[, 2],
  p = summary(fit)$coefficients[, 4]
)

gt(results) \%>\%
  fmt_number(c(Estimate, SE), decimals = 3) \%>\%
  gt_significance(Estimate, p)

# daggers instead of stars, at a single threshold
gt(results) \%>\%
  gt_significance(Estimate, p, levels = 0.05, symbols = "†",
                  legend_text = "† p < .05")
}

}
\seealso{
\code{\link[=gt_fmt_rank]{gt_fmt_rank()}}, which uses the same superscript approach for
ordinal suffixes.
}
