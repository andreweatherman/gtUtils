% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_color_ranks.R
\name{gt_color_ranks}
\alias{gt_color_ranks}
\title{Color ranked columns of a \code{gt} table}
\usage{
gt_color_ranks(
  gt_object,
  columns,
  rows = NULL,
  palette = c("#3D8B6E", "#9DC5A7", "#EDE0CC", "#DB9070", "#BE4D3A"),
  domain = NULL,
  reverse = FALSE,
  na_color = "white",
  autocolor_text = TRUE,
  pal_type = "discrete",
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The column or columns to color.}

\item{rows}{The rows to color. Either an expression evaluated against the
table's data, such as \code{mpg > 20}, or a numeric vector of row indices. Rows
left out keep their default background. If \code{NULL}, every row is colored.
Defaults to \code{NULL}.}

\item{palette}{A color palette to use. If you want a palette from \code{paletteer},
specify it as \code{package::palette}. Defaults to a five-color green-to-red ramp.}

\item{domain}{A length-2 numeric vector giving the value range mapped onto the
palette. If \code{NULL}, the range is taken from the selected columns and shared
across all of them, so rank 1 and the largest rank present anchor the two
ends of the palette. Defaults to \code{NULL}.}

\item{reverse}{Logical. Should the palette be reversed? Defaults to \code{FALSE}.}

\item{na_color}{Character. The color used for \code{NA} values. Defaults to \code{"white"}.}

\item{autocolor_text}{Logical. Should the text color be set automatically for
contrast against the fill? Defaults to \code{TRUE}.}

\item{pal_type}{Character. Either \code{"discrete"} or \code{"continuous"}, used when
applying \code{paletteer} palettes. Defaults to \code{"discrete"}.}

\item{...}{Additional arguments passed to \code{gt::data_color}.}
}
\value{
Returns a modified \code{gt} table with the selected columns filled by value.
}
\description{
A shorthand around \code{gt::data_color()} for filling columns that already hold
rank values, where 1 is best. It takes either a plain vector of hex colors or a
\code{paletteer} palette, and either an explicit domain or one derived from the
data. Values are colored as they are; no ranking is computed for you.
}
\details{
The whole cell background is filled. \code{\link[=gt_color_pills]{gt_color_pills()}} does the same job with
rounded pill spans instead.
}
\examples{
\dontrun{
library(gt)

ranked <- data.frame(
  model = rownames(head(mtcars, 6)),
  mpg_rank = rank(-head(mtcars, 6)$mpg),
  hp_rank = rank(head(mtcars, 6)$hp)
)

gt(ranked) \%>\% gt_color_ranks(c(mpg_rank, hp_rank))

# a paletteer palette, reversed
gt(ranked) \%>\%
  gt_color_ranks(mpg_rank, palette = "viridis::mako", pal_type = "continuous",
                 reverse = TRUE)
}

}
\seealso{
\code{\link[=gt_legend_continuous]{gt_legend_continuous()}} for explaining the scale, and \code{\link[=gt_color_pills]{gt_color_pills()}}
for a pill treatment instead of a full-cell fill.
}
