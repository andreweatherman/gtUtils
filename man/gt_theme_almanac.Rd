% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_almanac.R
\name{gt_theme_almanac}
\alias{gt_theme_almanac}
\title{Record-book theme for \code{gt} tables}
\usage{
gt_theme_almanac(
  gt_object,
  accent = "#8C2F1E",
  density = c("compact", "comfortable", "social"),
  stripe = "#F1F1EF",
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the row-group labels and the rule
above the table. Defaults to \code{"#8C2F1E"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"compact"}.}

\item{stripe}{Character. A hex color for the banded rows. Pass \code{NA} to switch
banding off and keep the rest of the theme. Defaults to \code{"#F1F1EF"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
The look of a printed statistical abstract. A slab body, narrow condensed
labels, tight rows, and row banding to walk the eye across a wide table.
}
\details{
It is the only theme in the package that bands its rows.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_almanac.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars, 12)) \%>\% gt_theme_almanac()

# banding off, cooler accent
gt(head(airquality, 15)) \%>\% gt_theme_almanac(stripe = NA, accent = "#1F3A5F")
}

}
