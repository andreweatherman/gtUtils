% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_broadsheet.R
\name{gt_theme_broadsheet}
\alias{gt_theme_broadsheet}
\title{Editorial serif theme for \code{gt} tables}
\usage{
gt_theme_broadsheet(
  gt_object,
  accent = "#A6081A",
  density = c("comfortable", "compact", "social"),
  paper = "white",
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the rule above the table and for the
row-group labels. Defaults to \code{"#A6081A"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{paper}{Character. The table background. Either \code{"white"} for a warm
off-white, \code{"salmon"} for the financial-press pink, or any hex color, in
which case the hairline color is left neutral. Defaults to \code{"white"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
Sets a serif body under small letterspaced sans column labels, with hairline
rules and no fills anywhere. Row groups are an uppercase label over a rule
rather than a colored band.
}
\details{
\code{accent} recolors the rule above the table and the row-group labels.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_broadsheet.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)

gt(head(mtcars[c("mpg", "hp", "wt")], 8)) \%>\% gt_theme_broadsheet()

# financial-press pink, sized for an image export
gt(head(airquality, 8)) \%>\%
  gt_theme_broadsheet(paper = "salmon", accent = "#0F5257", density = "social")
}

}
\seealso{
\code{\link[=gt_title_header]{gt_title_header()}} for a richer header block, and \code{\link[=gt_legend_continuous]{gt_legend_continuous()}} for
explaining a colored column.
}
