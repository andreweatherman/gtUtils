% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_fmt_tally.R
\name{gt_fmt_tally}
\alias{gt_fmt_tally}
\title{Combine count columns into a single cell in a \code{gt} table}
\usage{
gt_fmt_tally(
  gt_object,
  columns,
  separator = "-",
  label = NULL,
  share = FALSE,
  share_of = 1,
  share_location = c("inline", "column"),
  share_decimals = 1,
  share_label = "\%",
  share_prefix = " (",
  share_suffix = ")",
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The count columns to combine, in the order they should read.
Two or more.}

\item{separator}{Character. The string placed between the counts. Defaults to
\code{"-"}.}

\item{label}{Optional. A new label for the combined column. Defaults to
\code{NULL}, which leaves the existing label alone.}

\item{share}{Logical. Should one of the counts be shown as a share of the row
total? Defaults to \code{FALSE}.}

\item{share_of}{The column the share is computed for, either a position or a
name. Defaults to \code{1}, the first column.}

\item{share_location}{Character. Where the share goes. Either \code{"inline"} to
append it to the combined cell, or \code{"column"} to reuse the last of \code{columns}
for it. Defaults to \code{"inline"}.}

\item{share_decimals}{Integer. The number of decimal places for the share.
Defaults to \code{1}.}

\item{share_label}{Character. The label for the share column when
\code{share_location} is \code{"column"}. Defaults to \code{"\%"}.}

\item{share_prefix}{Character. The string placed before an inline share.
Defaults to \code{" ("}.}

\item{share_suffix}{Character. The string placed after an inline share.
Defaults to \code{")"}.}

\item{...}{Additional arguments passed to \code{gt::vec_fmt_percent}.}
}
\value{
Returns a modified \code{gt} table with the counts combined.
}
\description{
Merges two or more count columns into one \code{"32-5"} style cell, optionally with
one of them shown as a share of the row total. Any set of tallies works, from
wins and losses to tests passed and failed.
}
\details{
The counts are written into the first column and the rest are hidden, or the
last one is reused to carry the share, so the table ends up narrower than it
started.

\code{gt::cols_merge()} will join columns with a pattern and
\code{gt::cols_merge_n_pct()} will pair a count with a percentage column you have
already built. The difference here is that the share is computed for you from
the counts themselves.

A row is left alone if any of its counts are missing, so a partial tally is
never shown. The share is also left blank where the counts sum to zero.
}
\examples{
\dontrun{
library(gt)

suites <- data.frame(
  Suite = c("Parser", "Renderer", "Exporter"),
  Passed = c(142, 98, 211),
  Failed = c(8, 2, 17)
)

# renders as "142-8"
gt(suites) \%>\% gt_fmt_tally(c(Passed, Failed), label = "Result")

# renders as "142-8 (94.7\%)"
gt(suites) \%>\% gt_fmt_tally(c(Passed, Failed), share = TRUE)

# the failure rate instead, in its own column
gt(suites) \%>\%
  gt_fmt_tally(c(Passed, Failed), share = TRUE, share_of = "Failed",
               share_location = "column", share_label = "Fail rate")

# three counts, as in a league table
league <- data.frame(
  Club = c("Arsenal", "Chelsea"),
  W = c(26, 18), D = c(6, 10), L = c(6, 10)
)

gt(league) \%>\% gt_fmt_tally(c(W, D, L), label = "W-D-L")
}

}
\seealso{
\code{\link[=gt_fmt_rank]{gt_fmt_rank()}} for ordinal formatting.
}
