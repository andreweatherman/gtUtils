% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_fmt_rank.R
\name{gt_fmt_rank}
\alias{gt_fmt_rank}
\title{Format numbers as ordinals in a \code{gt} table}
\usage{
gt_fmt_rank(gt_object, columns, superscript = TRUE, suffix_size = "0.7em")
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The column or columns to format.}

\item{superscript}{Logical. Should the suffix be rendered as superscript?
Defaults to \code{TRUE}.}

\item{suffix_size}{Character. The size of the suffix, as a CSS size. Defaults
to \code{"0.7em"}.}
}
\value{
Returns a modified \code{gt} table with ordinal formatting applied.
}
\description{
Turns plain integers into ordinals: 1 becomes 1st, 2 becomes 2nd, 23 becomes
23rd. The suffix can be rendered as superscript, which is the usual treatment
in a ranked column.
}
\details{
Teens are handled correctly, so 11, 12 and 13 take "th" and not "st", "nd" and
"rd". Values that are not numeric are left alone.
}
\examples{
\dontrun{
library(gt)

ranked <- data.frame(
  model = rownames(head(mtcars, 5)),
  place = 1:5
)

gt(ranked) \%>\% gt_fmt_rank(place)

# flat, without the superscript
gt(ranked) \%>\% gt_fmt_rank(place, superscript = FALSE)
}

}
