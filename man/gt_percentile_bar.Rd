% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_percentile_bar.R
\name{gt_percentile_bar}
\alias{gt_percentile_bar}
\title{Draw percentile bars in a \code{gt} table}
\usage{
gt_percentile_bar(
  gt_object,
  columns,
  rows = NULL,
  domain = c(0, 100),
  scale = "auto",
  palette = c("#3661AD", "#C9C9C9", "#D22D49"),
  reverse = FALSE,
  pal_type = "discrete",
  track_color = "#E9E9E9",
  track_height = 6,
  marker_size = 22,
  text_color = "#FFFFFF",
  font_size = NULL,
  ring_color = NULL,
  ring_width = 2,
  full_track = TRUE,
  na_label = "—",
  na_track_color = NULL,
  na_text_color = "#9A9A9A",
  decimals = 0,
  width = 220
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The columns holding percentiles.}

\item{rows}{The rows to draw bars in. Either an expression evaluated against
the table's data, such as \code{pct > 50}, or a numeric vector of row indices.
Rows left out keep their raw value. If \code{NULL}, every row is drawn. Defaults
to \code{NULL}.}

\item{domain}{A length-2 numeric vector giving the percentile range. Defaults
to \code{c(0, 100)}.}

\item{scale}{How to handle a column stored as 0-1 proportions rather than
0-100 percentiles. \code{"auto"} maps a column whose values all fall in \verb{[0, 1]}
onto \code{domain}, so \code{0.72} is treated and printed as \code{72} against the default
range. \code{"none"} leaves the values untouched. A number multiplies every value
by it, for any other conversion. Defaults to \code{"auto"}.}

\item{palette}{Colors mapped across \code{domain} and used for both the fill and
the marker. Defaults to a blue-grey-red ramp.}

\item{reverse}{Logical. Should the palette be reversed? Defaults to \code{FALSE}.}

\item{pal_type}{Character. Which \code{paletteer} registry to look a
\code{package::palette} string up in, \code{"discrete"} or \code{"continuous"}. The other
registry is tried as a fallback, so this rarely needs setting. Defaults to
\code{"discrete"}.}

\item{track_color}{Character. The color of the unfilled track. Defaults to
\code{"#E9E9E9"}.}

\item{track_height}{Numeric. The track thickness in pixels. Defaults to \code{6}.}

\item{marker_size}{Numeric. The marker diameter in pixels. Defaults to \code{22}.}

\item{text_color}{Character. The color of the number inside the marker.
Defaults to \code{"#FFFFFF"}.}

\item{font_size}{Numeric. The size of the number in pixels. If \code{NULL}, it is
set from \code{marker_size}. Defaults to \code{NULL}.}

\item{ring_color}{Character. A ring drawn around the marker, which separates it
from the fill. \code{NULL} for none. Defaults to \code{NULL}.}

\item{ring_width}{Numeric. The ring thickness in pixels. Defaults to \code{2}.}

\item{full_track}{Logical. Should the track run the full width, or stop at the
marker? Defaults to \code{TRUE}.}

\item{na_label}{Character. What to show for a missing percentile, such as
\code{"Not qualified"} or \code{"NR"}. The track is drawn in two segments with the
label centered between them, so the row keeps its rhythm and cannot be read
as a percentile of zero. \code{NULL} draws an unbroken empty track with no text.
Defaults to an em dash.}

\item{na_track_color}{Character. The track color for missing rows. If \code{NULL},
\code{track_color} is used. Defaults to \code{NULL}.}

\item{na_text_color}{Character. The color of \code{na_label}. Defaults to
\code{"#9A9A9A"}.}

\item{decimals}{Integer. Decimal places on the number. Defaults to \code{0}.}

\item{width}{Numeric. The column width in pixels. Defaults to \code{220}.}
}
\value{
Returns a modified \code{gt} table with percentile bars.
}
\description{
Draws a percentile as a filled track with a circular marker at the tip of the
fill, the value printed inside it. The bar, the marker, and the number are all
CSS, so the whole thing stays vector at any export scale.
}
\details{
\code{gtExtras::gt_plt_percentile()} covers percentiles as a ggplot dot plot
rendered to a raster image. This is the filled-bar-and-marker treatment
instead.

The track is inset by half the marker so that a value at either extreme keeps
the marker inside the cell rather than hanging over the edge. The fill and the
marker take the same color, mapped from the value.
}
\examples{
\dontrun{
library(gt)

metrics <- data.frame(
  metric = c("Barrel \%", "Exit velocity", "Chase rate", "Whiff \%"),
  pct = c(94, 41, 72, 18)
)

gt(metrics) \%>\% gt_percentile_bar(pct)

# the same values stored 0-1 render identically under the default scale = "auto"
metrics$pct <- metrics$pct / 100
gt(metrics) \%>\% gt_percentile_bar(pct)

# a row that did not qualify
metrics$pct[2] <- NA
gt(metrics) \%>\% gt_percentile_bar(pct, na_label = "Not qualified")
}

}
