% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_social_crop.R
\name{gt_social_crop}
\alias{gt_social_crop}
\title{Save a \code{gt} table onto a fixed-ratio canvas}
\usage{
gt_social_crop(
  data,
  file = NULL,
  aspect_ratio = "1:1",
  bg = "white",
  whitespace = 60,
  gravity = "center",
  zoom = 2,
  expand = 5,
  width = NULL
)
}
\arguments{
\item{data}{A \code{gt} table object to save.}

\item{file}{Optional. A path to write the image to. Defaults to \code{NULL}.}

\item{aspect_ratio}{Character or numeric. The target ratio, given as \code{"1:1"},
\code{"16:9"}, \code{"4:5"}, or a number. Defaults to \code{"1:1"}.}

\item{bg}{Character. The background color of the canvas. Defaults to \code{"white"}.}

\item{whitespace}{Numeric. The amount of whitespace to leave around the table
before padding out to the ratio. Defaults to \code{60}.}

\item{gravity}{Character. Where the table sits on the canvas, passed to
\code{magick::image_extent}. Defaults to \code{"center"}.}

\item{zoom}{Numeric. The rendering zoom factor. Defaults to \code{2}.}

\item{expand}{Numeric. The pixel expansion passed to the underlying save.
Defaults to \code{5}.}

\item{width}{Optional. A final output width in pixels. The finished canvas is
scaled to it, keeping the aspect ratio, so a series of posts can share one
width. Defaults to \code{NULL}.}
}
\value{
Returns the cropped and padded image file.
}
\description{
A variant of \code{\link[=gt_save_crop]{gt_save_crop()}} that centers the trimmed table on a canvas of a
given aspect ratio, for posting to social platforms. The table itself is never
cropped; the short side of the canvas is expanded until the ratio is met.
}
\examples{
\dontrun{
library(gt)

gt(head(mtcars)) \%>\%
  gt_theme_broadsheet(density = "social") \%>\%
  gt_social_crop("mtcars.png", aspect_ratio = "4:5")

# match the canvas to a dark theme
gt(head(mtcars)) \%>\%
  gt_theme_midnight() \%>\%
  gt_social_crop("dark.png", bg = "#0C0D10")
}

}
\seealso{
\code{\link[=gt_save_crop]{gt_save_crop()}} for a plain trimmed save.
}
