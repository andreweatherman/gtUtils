% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_save_batch.R
\name{gt_save_batch}
\alias{gt_save_batch}
\title{Save a matched set of images, one per group}
\usage{
gt_save_batch(
  data,
  group,
  fn,
  file,
  dir = ".",
  match_width = TRUE,
  bg = "white",
  whitespace = 50,
  zoom = 2,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame to split.}

\item{group}{The column to split on.}

\item{fn}{A function building one table. It is called as \code{fn(df, group)},
where \code{df} is that group's rows and \code{group} is its value, and it must return
a \code{gt_tbl}.}

\item{file}{Character. A file name pattern containing \code{{group}}, which is
replaced by the group value, as \code{"net-{group}.png"}.}

\item{dir}{Character. The directory to write into. Created if it does not
exist. Defaults to \code{"."}.}

\item{match_width}{Logical. Should every image be padded to the width of the
widest one, so a posted series shares one width? Defaults to \code{TRUE}.}

\item{bg}{Character. The background color of the padding. Defaults to
\code{"white"}.}

\item{whitespace}{Numeric. Padding left around each table, in pixels. Defaults
to \code{50}.}

\item{zoom}{Numeric. The rendering zoom factor. Defaults to \code{2}.}

\item{quiet}{Logical. Suppress the per-group progress message. Defaults to
\code{FALSE}.}
}
\value{
Invisibly, a character vector of the files written.
}
\description{
Splits data by a column, builds a table for each group with a function you
supply, and writes one image per group. The images are padded to a common
width so a posted series does not come out at ragged sizes.
}
\details{
A group whose table fails to build does not abandon the run. The error is
collected, the remaining groups are written, and a warning at the end names
every group that failed.

Group values are used in file names with anything awkward replaced by a dash,
so a group called \code{"North / East"} writes to \code{north-east}.

Without \code{match_width}, each image is trimmed to its own content, so a set
posted together can come out at different widths. With it, every image is
padded to the width of the widest before its border is added.
}
\examples{
\dontrun{
library(gt)

build <- function(df, group) {
  gt(df[c("mpg", "hp", "wt")]) \%>\%
    gt_theme_broadsheet() \%>\%
    tab_header(title = paste(group, "cylinders"))
}

gt_save_batch(mtcars, cyl, build, "cars-{group}.png", dir = "out")
}

}
\seealso{
\code{\link[=gt_grid]{gt_grid()}} for the same split composed into one image instead.
}
