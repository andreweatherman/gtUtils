% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_preview.R
\name{gt_theme_preview}
\alias{gt_theme_preview}
\title{Preview data in every theme}
\usage{
gt_theme_preview(
  data,
  themes = NULL,
  n = 5,
  ncol = 3,
  density = "compact",
  file = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame. A \code{gt} table is also accepted, in which case its
underlying data is used.}

\item{themes}{Character. The theme functions to show, by name. If \code{NULL},
every \verb{gt_theme_*} in the package is used. Defaults to \code{NULL}.}

\item{n}{Integer. How many rows of \code{data} to show in each panel. Defaults to
\code{5}.}

\item{ncol}{Integer. The number of panels across. Defaults to \code{3}.}

\item{density}{Character. A \code{density} passed to every theme, so the panels are
comparable. If \code{NULL}, each theme uses its own default. Defaults to
\code{"compact"}.}

\item{file}{Optional. A path to write a PNG to. If \code{NULL}, the grid is
returned for the viewer. Defaults to \code{NULL}.}

\item{...}{Further arguments passed to \code{\link[=gt_grid]{gt_grid()}}, such as \code{gap} or \code{bg}.}
}
\value{
Displays the grid in the viewer, or writes it to \code{file}.
}
\description{
Renders the same few rows through each \verb{gt_theme_*} function in the package
and lays the results out in a grid, one panel per theme, labeled with its name.
}
\details{
Each panel is captioned with the theme's name in a neutral style, set outside
the table. Using each theme's own heading instead would let a wide display
title stretch its panel out of shape.

Themes that take extra arguments, such as \code{style} on \code{\link[=gt_theme_sofa]{gt_theme_sofa()}}, are
shown at their defaults.
}
\examples{
\dontrun{
gt_theme_preview(mtcars[c("mpg", "cyl", "hp")])

# a subset, sized for a wide screenshot
gt_theme_preview(
  iris,
  themes = c("gt_theme_broadsheet", "gt_theme_swiss", "gt_theme_midnight"),
  ncol = 3, file = "themes.png"
)
}

}
\seealso{
\code{\link[=gt_grid]{gt_grid()}}, which does the layout.
}
