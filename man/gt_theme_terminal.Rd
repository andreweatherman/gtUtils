% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_terminal.R
\name{gt_theme_terminal}
\alias{gt_theme_terminal}
\title{Data-terminal theme for \code{gt} tables}
\usage{
gt_theme_terminal(
  gt_object,
  accent = "#FFB86C",
  density = c("compact", "comfortable", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the column labels, row groups, and
the top rule. Defaults to amber \code{"#FFB86C"}; \code{"#7EE787"} gives a
green-phosphor variant.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"compact"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
A dense monospace theme in the register of a trading terminal. Dark background,
amber labels, a rule on every row, and tight padding.
}
\details{
The monospace face has fixed-width digits already, so numeric columns line up
without any extra formatting.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_terminal.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars, 12)) \%>\% gt_theme_terminal()
gt(head(airquality, 15)) \%>\% gt_theme_terminal(accent = "#7EE787")
}

}
\seealso{
\link{pal_midnight} for a color scale that survives a dark ground.
}
