% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_wrap_labels.R
\name{gt_wrap_labels}
\alias{gt_wrap_labels}
\title{Break long column labels across lines in a \code{gt} table}
\usage{
gt_wrap_labels(
  gt_object,
  columns = gt::everything(),
  width = 12,
  balance = TRUE
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The columns whose labels should wrap. Defaults to
\code{gt::everything()}.}

\item{width}{Integer. The target line length in characters. Defaults to \code{12}.}

\item{balance}{Logical. Should the lines be evened out rather than filled
greedily left to right? Defaults to \code{TRUE}.}
}
\value{
Returns a modified \code{gt} table with the selected labels wrapped.
}
\description{
Wraps a column label onto several short lines so a wide heading stops forcing a
narrow column wider than its data needs. "Strength of Schedule" over a
two-digit column becomes three stacked lines instead of one long one.
}
\details{
The hand-rolled version is \code{gt::cols_label(x = gt::html("A<br>B"))}, written
out per column. This wraps at a width for you, and by default balances the
lines so they come out close to even rather than a long first line over a
short last one.

A label of one word, or one already shorter than \code{width}, is left alone. The
wrap is on whitespace only, so a single long word is never split.
}
\examples{
\dontrun{
library(gt)

scores <- data.frame(
  name = c("Item A", "Item B"),
  sos = c(0.62, 0.48),
  adj = c(112.4, 98.1)
)

gt(scores) \%>\%
  cols_label(sos = "Strength of Schedule", adj = "Adjusted Efficiency") \%>\%
  gt_wrap_labels(c(sos, adj), width = 10)
}

}
