% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_delta.R
\name{gt_delta}
\alias{gt_delta}
\title{Add a computed change column to a \code{gt} table}
\usage{
gt_delta(
  gt_object,
  from,
  to,
  column_label = "Change",
  percent = FALSE,
  decimals = 1,
  arrows = FALSE,
  color = TRUE,
  color_positive = "#1B7837",
  color_negative = "#B2182B",
  color_neutral = NULL,
  force_sign = TRUE,
  after = NULL
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{from}{The starting column, a single numeric column.}

\item{to}{The ending column, a single numeric column. The change is
\code{to - from}.}

\item{column_label}{Character. The label for the new column. Defaults to
\code{"Change"}.}

\item{percent}{Logical. Should the change be shown as a percent of \code{from}
rather than an absolute difference? Defaults to \code{FALSE}.}

\item{decimals}{Integer. The number of decimal places. Defaults to \code{1}.}

\item{arrows}{Logical. Should an up or down triangle lead the value in place
of a sign? Defaults to \code{FALSE}.}

\item{color}{Logical. Should the values be colored by direction? Defaults to
\code{TRUE}.}

\item{color_positive}{Character. The color for an increase. Defaults to a
green, \code{"#1B7837"}.}

\item{color_negative}{Character. The color for a decrease. Defaults to a red,
\code{"#B2182B"}.}

\item{color_neutral}{Optional. The color for no change. Defaults to \code{NULL},
which leaves zero the table's normal text color.}

\item{force_sign}{Logical. Should a plus be shown on an increase? Ignored when
\code{arrows} is \code{TRUE}, since the arrow carries the direction. Defaults to \code{TRUE}.}

\item{after}{Optional. The column the new one is placed after, a position or a
name. Defaults to \code{NULL}, which places it after \code{to}.}
}
\value{
Returns a modified \code{gt} table with the change column added.
}
\description{
Takes two numeric columns and inserts a new column holding the change between
them, signed and colored by direction. The change is \code{to} minus \code{from}, so a
later period as \code{to} and an earlier one as \code{from} gives a positive number when
the value has grown. The arithmetic, the formatting, and the sign coloring
happen in one call.
}
\details{
\code{gt::fmt_number(force_sign = TRUE)} formats a difference you have already
worked out, and \code{gtExtras::gt_fa_rank_change()} handles movement in a rank
column. This computes the delta from two columns, formats it, and colors it by
sign together.

A row is left blank where either \code{from} or \code{to} is missing, and where a
percent change divides by a \code{from} of zero. When \code{arrows} is \code{TRUE} the value
is shown as a magnitude behind the triangle, so a decrease reads as a down
triangle in front of a positive number.
}
\examples{
\dontrun{
library(gt)

revenue <- data.frame(
  Segment = c("Hardware", "Software", "Services"),
  Q1 = c(482, 331, 198),
  Q2 = c(515, 302, 246)
)

# absolute change, colored by sign
gt(revenue) \%>\% gt_delta(Q1, Q2)

# as a percent of Q1, with arrows
gt(revenue) \%>\% gt_delta(Q1, Q2, percent = TRUE, arrows = TRUE)
}

}
\seealso{
\code{\link[=gt_scale_note]{gt_scale_note()}} for disclosing a divided scale.
}
