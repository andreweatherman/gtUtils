% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_grid.R
\name{gt_grid}
\alias{gt_grid}
\title{Arrange several \code{gt} tables in a grid}
\usage{
gt_grid(
  tables = NULL,
  ncol = 2,
  labels = NULL,
  label_style = list(),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  source_note = NULL,
  caption_rule = FALSE,
  title_style = list(),
  subtitle_style = list(),
  caption_style = list(),
  source_note_style = list(),
  gap = 24,
  align = c("top", "center", "bottom"),
  file = NULL,
  bg = "white",
  whitespace = 50,
  zoom = 2
)
}
\arguments{
\item{tables}{A list of \code{gt} table objects.}

\item{ncol}{Integer. The number of tables across. The number of rows follows
from the length of \code{tables}. Defaults to \code{2}.}

\item{labels}{Character. An optional caption above each table, recycled
against \code{tables}. Rendered in a neutral style rather than each table's own,
and placed outside the table so a wide label does not stretch its panel.
Defaults to \code{NULL}.}

\item{label_style}{A list of style options for the per-table captions. See
\code{title_style} for the keys. Defaults to an empty list.}

\item{title}{Character. An optional heading above the whole grid. Set this
instead of giving each table its own \code{gt::tab_header()}. Defaults to \code{NULL}.}

\item{subtitle}{Character. An optional line below \code{title}. Defaults to \code{NULL}.}

\item{caption}{Character. An optional note below the grid. Defaults to \code{NULL}.}

\item{source_note}{Character. An optional second line below \code{caption},
right-aligned by default. Set both, with \code{caption_rule = TRUE}, for the
split caption \code{\link[=gt_538_caption]{gt_538_caption()}} gives a single table. Defaults to \code{NULL}.}

\item{caption_rule}{Logical. Should a hairline rule sit between \code{caption} and
\code{source_note}? Defaults to \code{FALSE}.}

\item{title_style, subtitle_style, caption_style, source_note_style}{Lists of
style options. Recognized keys are \code{font} (a Google font name),
\code{size}, \code{color}, \code{weight}, \code{italic}, \code{spacing} (letter spacing), \code{transform}
(such as \code{"uppercase"}), and \code{align}, plus \code{line_height}, \code{margin_top},
\code{margin_bottom}, \code{padding_top}, and \code{padding_bottom} for nudging the blocks
closer together or further apart. Any key you leave out keeps its default,
so \code{title_style = list(size = "34px")} changes only the size. Same
convention as \code{\link[=gt_title_header]{gt_title_header()}}. Lengths take a number, read as pixels, or
a CSS string such as \code{"2rem"}; a negative margin pulls an element up tight
against the one above it.}

\item{gap}{Numeric. The space between tables in pixels. Defaults to \code{24}.}

\item{align}{Character. How tables of differing height line up within a row.
Either \code{"top"}, \code{"center"}, or \code{"bottom"}. Defaults to \code{"top"}.}

\item{file}{Optional. A path to write a PNG to. If \code{NULL}, the grid is
returned for the viewer instead. Defaults to \code{NULL}.}

\item{bg}{Character. The background color, used when saving. Defaults to
\code{"white"}.}

\item{whitespace}{Numeric. Padding left around the grid when saving, in
pixels. Defaults to \code{50}.}

\item{zoom}{Numeric. The rendering zoom factor used when saving. Defaults to
\code{2}.}
}
\value{
Displays the grid in the viewer, or writes it to \code{file}.
}
\description{
Lays a list of tables out in rows and columns as small multiples, one table
per region or per quarter. \code{gtExtras::gt_two_column_layout()} handles exactly
two tables and \code{\link[=gt_stack_tables]{gt_stack_tables()}} stacks any number of them vertically. This
covers the rest.
}
\details{
Unlike most of this package, this does not return a \code{gt} table. Separate
tables have their own columns, widths, and headers, so there is no single
table to hand back; the grid is assembled as HTML. That makes it a last step,
after each table is themed and formatted, and it is why saving happens here
through \code{file} rather than through \code{\link[=gt_save_crop]{gt_save_crop()}}.

Each table also keeps its own heading. Use \code{title} when the tables are blocks
of a single thing, as with a long ranking split in two, and per-table
\code{gt::tab_header()}s when they are genuinely separate exhibits.

Saving needs the \code{webshot2} package.
}
\examples{
\dontrun{
library(gt)

by_cyl <- lapply(split(mtcars, mtcars$cyl), function(d) {
  gt(head(d[c("mpg", "hp", "wt")], 5)) \%>\%
    gt_theme_broadsheet() \%>\%
    tab_header(title = paste(d$cyl[[1]], "cylinders"))
})

gt_grid(by_cyl, ncol = 2)

# one heading over the whole sheet
gt_grid(by_cyl, ncol = 3, title = "Fuel economy by cylinder count",
        subtitle = "1974 Motor Trend road tests", caption = "Data: mtcars")

# a 538-style split caption under the whole sheet
gt_grid(by_cyl, ncol = 3,
        title = "Fuel economy by cylinder count",
        caption = gt::md("Cars are grouped by **cylinder count**."),
        source_note = "Data: mtcars | Andrew Weatherman",
        caption_rule = TRUE)

# tighten the subtitle up under the title, and open the gap to the grid
gt_grid(by_cyl, ncol = 3, title = "Fuel economy", subtitle = "Motor Trend, 1974",
        title_style = list(margin_bottom = -2),
        subtitle_style = list(margin_bottom = 24))

# styled without writing any css
gt_grid(by_cyl, ncol = 3,
        title = "Fuel economy by cylinder count",
        subtitle = "1974 Motor Trend road tests",
        title_style = list(font = "Oswald", size = 34, transform = "uppercase"),
        subtitle_style = list(italic = TRUE, color = "#8A8A8A"))

# straight to an image
gt_grid(by_cyl, ncol = 3, file = "cylinders.png", bg = "#FBFAF7")
}

}
\seealso{
\code{\link[=gt_stack_tables]{gt_stack_tables()}} for a vertical stack, and \code{\link[=gt_snake]{gt_snake()}} for
folding a single long table into blocks.
}
