% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_row_accent.R
\name{gt_row_accent}
\alias{gt_row_accent}
\title{Put a colored bar on the leading edge of each row}
\usage{
gt_row_accent(
  gt_object,
  column,
  palette = NULL,
  rows = NULL,
  width = 4,
  side = c("left", "right"),
  hide = TRUE,
  na_color = "transparent"
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{column}{The column the color is keyed to. Either a column already
holding colors, or a grouping column used together with \code{palette}.}

\item{palette}{Optional. Colors to map \code{column} onto. A named vector maps
values explicitly, as \code{c(ACC = "#003366", SEC = "#B8232F")}. An unnamed
vector is assigned across the sorted unique values and recycled. If \code{NULL},
\code{column} is taken to hold colors already. Defaults to \code{NULL}.}

\item{rows}{The rows to accent. Either an expression evaluated against the
table's data, such as \code{conf == "ACC"} or \code{net <= 10}, or a numeric vector of
row indices, or a single row number. If \code{NULL}, every row is accented.
Defaults to \code{NULL}.}

\item{width}{Numeric. The bar width in pixels. Defaults to \code{4}.}

\item{side}{Character. Which edge the bar sits on, \code{"left"} or \code{"right"}.
Defaults to \code{"left"}.}

\item{hide}{Logical. Should \code{column} be hidden once the bar is drawn? Usually
what you want when it holds hex codes. Defaults to \code{TRUE}.}

\item{na_color}{Character. The color for rows where the key is missing.
Defaults to \code{"transparent"}, which draws no bar.}
}
\value{
Returns a modified \code{gt} table with a color bar on each row.
}
\description{
Draws a short color bar down the left edge of every row, keyed to a column in
the data such as a team, category, or brand color. It serves as a per-row key
without taking up a column.
}
\details{
It draws the same bar \code{\link[=gt_spotlight]{gt_spotlight()}} uses for emphasis, applied per row here.
\code{gtExtras::gt_highlight_rows()} fills the whole row instead.

The bar is drawn as a cell border on the leftmost rendered column, or on the
stub when the table has one, so it lines up with the row rather than sitting
inside a column of its own.

Rows sharing a color are styled together rather than one at a time, so a long
table with a handful of categories stays cheap.
}
\examples{
\dontrun{
library(gt)

teams <- data.frame(
  team = c("Duke", "Kansas", "Auburn", "Houston"),
  conf = c("ACC", "B12", "SEC", "B12"),
  net = c(10, 20, 5, 1)
)

# keyed to a grouping column
gt(teams) \%>\%
  gt_row_accent(conf, palette = c(ACC = "#003366", B12 = "#C8102E",
                                  SEC = "#B8232F"))

# or straight from a column of colors
teams$color <- c("#003366", "#C8102E", "#B8232F", "#C8102E")
gt(teams) \%>\% gt_row_accent(color)

# only some rows: by expression, by indices, or by one row number
gt(teams) \%>\% gt_row_accent(color, rows = net <= 10)
gt(teams) \%>\% gt_row_accent(color, rows = c(1, 3))
gt(teams) \%>\% gt_row_accent(color, rows = 2)
}

}
\seealso{
\code{\link[=gt_spotlight]{gt_spotlight()}} for emphasis rather than a key.
}
