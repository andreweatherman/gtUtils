% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_highlight_cells.R
\name{gt_highlight_cells}
\alias{gt_highlight_cells}
\title{Fill individual cells in a \code{gt} table by a condition}
\usage{
gt_highlight_cells(
  gt_object,
  columns,
  condition,
  fill = "#FFF3B0",
  text_color = NULL,
  bold = FALSE,
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The block of columns to test, the body of the grid.}

\item{condition}{How a cell is chosen. Either a function or a right-hand-side
formula applied to each column and returning a logical vector (\code{~ .x > 0.7},
\code{~ grepl("home", .x)}, \code{~ is.na(.x)}), or a logical matrix or data frame the
same shape as \code{columns} for a mask computed ahead of time.}

\item{fill}{Character. The cell fill color. Defaults to \code{"#FFF3B0"}, a soft
yellow.}

\item{text_color}{Optional. A hex color for the text in filled cells. Defaults
to \code{NULL}, which leaves the text color alone.}

\item{bold}{Logical. Should filled cells be bolded? Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{gt::cell_text()} for the filled
cells, such as \code{style = "italic"}.}
}
\value{
Returns a modified \code{gt} table with the matching cells filled.
}
\description{
Fills each cell in a block of columns that meets a condition and leaves the
rest alone. It is the per-cell counterpart to \code{gtExtras::gt_highlight_cols()}
and \code{gtExtras::gt_highlight_rows()}, which fill a whole column or a whole row.
The condition is tested against every cell on its own, so a scattered set of
cells can be filled in one call.
}
\details{
A single \code{gt::tab_style()} with \code{gt::cells_body()} cannot do this, since it
reads \code{rows} and \code{columns} as a rectangle, every selected row crossed with
every selected column. Filling a diagonal, a checker pattern, or any scatter
otherwise takes a loop over the columns, one pass each. This wraps that loop.

Each selected column is filled independently, so the pattern can be a
diagonal, a checker, or any scatter of cells.

The condition reads the underlying data rather than the rendered cell, so a
test like \code{~ grepl("home", .x)} sees whatever value the column holds, HTML and
all. A formula or function must return one logical per row, and \code{NA} counts as
not matched. For more than one color, layer the calls, one per color.
}
\examples{
\dontrun{
library(gt)

# a correlation matrix: flag strong pairs, but not the diagonal of ones
m <- round(cor(mtcars[, c("mpg", "disp", "hp", "wt", "qsec")]), 2)
cordf <- data.frame(var = rownames(m), m, row.names = NULL, check.names = FALSE)
cordf \%>\%
  gt(rowname_col = "var") \%>\%
  gt_highlight_cells(-var, ~ .x > 0.7 & .x < 1, fill = "#FFD1A9") \%>\%
  gt_highlight_cells(-var, ~ .x < -0.7, fill = "#A9D0FF")

# a schedule matrix: home games one color, byes another
sched <- data.frame(
  team = c("Alpha", "Bravo", "Charlie"),
  wk1 = c("vs X", "@ Y", "BYE"),
  wk2 = c("@ W", "vs V", "@ U")
)
gt(sched) \%>\%
  gt_highlight_cells(c(wk1, wk2), ~ grepl("^vs", .x), fill = "#CCE7F5") \%>\%
  gt_highlight_cells(c(wk1, wk2), ~ .x == "BYE", fill = "#D9D9D9")
}

}
\seealso{
\code{gtExtras::gt_highlight_cols()} and \code{gtExtras::gt_highlight_rows()}
for whole-column and whole-row fills.
}
