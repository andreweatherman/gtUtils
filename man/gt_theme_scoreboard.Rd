% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_scoreboard.R
\name{gt_theme_scoreboard}
\alias{gt_theme_scoreboard}
\title{Broadcast-graphics theme for \code{gt} tables}
\usage{
gt_theme_scoreboard(
  gt_object,
  accent = "#0E1621",
  density = c("compact", "comfortable", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the header band. The label color
adapts to it. Defaults to \code{"#0E1621"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"compact"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
A solid color band carrying condensed uppercase labels, over tight rows, after
a television lower-third or a stadium scoreboard.
}
\details{
\code{accent} sets the band itself, so one argument restyles the whole table to a
brand color. The label color is checked against the band and flipped to dark
if the accent is pale.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_scoreboard.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)
gt(head(mtcars)) \%>\% gt_theme_scoreboard()

# a brand color carries the whole table
gt(head(mtcars)) \%>\% gt_theme_scoreboard(accent = "#0F766E")
}

}
\seealso{
\code{\link[=gt_spotlight]{gt_spotlight()}} for picking out a row, and \code{\link[=gt_fmt_rank]{gt_fmt_rank()}} for ordinals.
}
