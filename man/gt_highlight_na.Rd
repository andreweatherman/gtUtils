% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_highlight_na.R
\name{gt_highlight_na}
\alias{gt_highlight_na}
\title{Style and relabel missing values in a \code{gt} table}
\usage{
gt_highlight_na(
  gt_object,
  columns = gt::everything(),
  fill = "#F0F0F0",
  text_color = NULL,
  bold = FALSE,
  italic = FALSE,
  missing_text = NULL,
  na_strings = "NA",
  ignore_case = FALSE,
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The column or columns to check. Defaults to all of them.}

\item{fill}{Character. A hex color for the cell fill behind missing values.
Defaults to \code{"#F0F0F0"}.}

\item{text_color}{Optional. A hex color for the text of missing values.
Defaults to \code{NULL}.}

\item{bold}{Logical. Should missing values be bolded? Defaults to \code{FALSE}.}

\item{italic}{Logical. Should missing values be italicized? Defaults to \code{FALSE}.}

\item{missing_text}{Optional. Replacement text for missing values, such as
\code{"--"} or \code{"Not reported"}. Defaults to \code{NULL}, which leaves the text alone.}

\item{na_strings}{A character vector of strings to treat as missing alongside
real \code{NA}. Defaults to \code{"NA"}.}

\item{ignore_case}{Logical. Should \code{na_strings} be matched case-insensitively?
Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{gt::cell_text}.}
}
\value{
Returns a modified \code{gt} table with missing values styled.
}
\description{
Highlights missing cells and can relabel them in the same call.
\code{gt::sub_missing()} substitutes text only, and only for real \code{NA} values.
}
\details{
Alongside real \code{NA}, this also catches values that are literally the string
\code{"NA"}, a common artifact of reading a CSV and a frequent reason
\code{sub_missing()} appears to do nothing. Widen \code{na_strings} to catch other
placeholders such as \code{"-"} or \code{"N/A"}.
}
\examples{
\dontrun{
library(gt)

gt(head(airquality, 10)) \%>\% gt_highlight_na(c(Ozone, Solar.R))

# relabel as well as highlight
gt(head(airquality, 10)) \%>\%
  gt_highlight_na(c(Ozone, Solar.R), missing_text = "not recorded",
                  italic = TRUE, fill = "#FFF8E1")

# also catch placeholder strings left behind by a CSV import
gt(head(airquality, 10)) \%>\%
  gt_highlight_na(everything(), na_strings = c("NA", "N/A", "-"))
}

}
\seealso{
\code{\link[=gt_outliers]{gt_outliers()}} for flagging values that are present but suspect.
}
