% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_watermark.R
\name{gt_watermark}
\alias{gt_watermark}
\title{Put a watermark behind a \code{gt} table}
\usage{
gt_watermark(
  gt_object,
  text = NULL,
  image = NULL,
  opacity = 0.06,
  size = "60\%",
  position = "center",
  color = "#000000",
  angle = 0,
  font = "Helvetica, Arial, sans-serif"
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{text}{Optional. Text to render as the watermark. Defaults to \code{NULL}.}

\item{image}{Optional. A path to a PNG, JPEG, or SVG file to use instead of
\code{text}. Defaults to \code{NULL}.}

\item{opacity}{Numeric. How faint the watermark is, from 0 to 1. Defaults to
\code{0.06}.}

\item{size}{Character. The width of the watermark relative to the table, as a
CSS background size such as \code{"60\%"}. Defaults to \code{"60\%"}.}

\item{position}{Character. Where it sits, as a CSS background position such as
\code{"center"} or \code{"right bottom"}. Defaults to \code{"center"}.}

\item{color}{Character. The text color, used only with \code{text}. Defaults to
\code{"#000000"}.}

\item{angle}{Numeric. Rotation in degrees, used only with \code{text}. Defaults to
\code{0}.}

\item{font}{Character. The font family for \code{text}. Defaults to a system
sans-serif.}
}
\value{
Returns a modified \code{gt} table with a watermark behind the body.
}
\description{
Sets a wordmark or a logo behind the table body, faint enough to read through.
}
\details{
A text watermark is drawn as an inline SVG rather than as HTML, because a
background image is the only way to sit behind the table body without taking
up a row. An SVG background cannot load the page's webfonts, so \code{font} has to
name a face installed on the machine rendering the table.

The watermark sits behind the body cells, so it will be hidden wherever a cell
has its own fill, from row striping or from \code{gt::data_color()}.
}
\examples{
\dontrun{
library(gt)

gt(head(mtcars)) \%>\% gt_watermark(text = "DRAFT", angle = -30, opacity = 0.08)

gt(head(mtcars)) \%>\%
  gt_watermark(image = "logo.png", size = "40\%", position = "right bottom")
}

}
\seealso{
\code{\link[=gt_social_tag]{gt_social_tag()}} for visible attribution in the source note.
}
