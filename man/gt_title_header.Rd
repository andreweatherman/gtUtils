% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_title_header.R
\name{gt_title_header}
\alias{gt_title_header}
\title{Build a styled header block for a \code{gt} table}
\usage{
gt_title_header(
  gt_object,
  title,
  subtitle = NULL,
  kicker = NULL,
  date = NULL,
  kicker_style = list(),
  title_style = list(),
  subtitle_style = list(),
  date_style = list()
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{title}{Character. The title text.}

\item{subtitle}{Optional. The subtitle text. Defaults to \code{NULL}.}

\item{kicker}{Optional. A short line above the title. Defaults to \code{NULL}.}

\item{date}{Optional. A date shown beneath the subtitle. A \code{Date} is formatted
as, for example, "July 21, 2026"; anything else is shown as given. Defaults
to \code{NULL}.}

\item{kicker_style}{A named list styling the kicker. See Styling. Defaults to
an empty list.}

\item{title_style}{A named list styling the title. Defaults to an empty list.}

\item{subtitle_style}{A named list styling the subtitle. Defaults to an empty
list.}

\item{date_style}{A named list styling the date. Defaults to an empty list.}
}
\value{
Returns a modified \code{gt} table with the header block applied.
}
\description{
Wraps \code{gt::tab_header()} to produce a fuller header: an optional kicker above
the title, the title, an optional subtitle, and an optional date. Each of the
four is styled independently through its own list, so the typography can be set
without writing the \code{tab_style()} calls yourself.
}
\details{
Google fonts named in the style lists are loaded through \code{gt}'s own machinery,
so they survive \code{gtsave()} as well as the HTML output.

\code{gt_title_header()} calls \code{gt::tab_header()}, which replaces the whole header.
Paired with \code{\link[=gt_legend_continuous]{gt_legend_continuous()}} at \code{location = "top"}, call this one
first or it will overwrite the legend.
}
\section{Styling}{


Each \verb{*_style} argument takes a named list. Recognized keys are \code{font} (a
Google font name), \code{size}, \code{color}, \code{weight}, \code{italic}, \code{spacing} (letter
spacing), \code{transform} (such as \code{"uppercase"}), and \code{align}, plus \code{line_height},
\code{margin_top}, \code{margin_bottom}, \code{padding_top}, and \code{padding_bottom} for nudging
the blocks closer together or further apart. Any key you leave out keeps its
default. Lengths take a number, read as pixels, or a CSS string such as
\code{"2rem"}. This is the same convention used by \code{\link[=gt_grid]{gt_grid()}}, \code{\link[=gt_legend_continuous]{gt_legend_continuous()}},
and \code{\link[=gt_legend_discrete]{gt_legend_discrete()}}.
}

\examples{
\dontrun{
library(gt)

gt(head(mtcars[c("mpg", "hp", "wt")], 6)) \%>\%
  gt_title_header(
    title = "Fuel economy and power",
    subtitle = "A sample of the 1974 Motor Trend road tests",
    kicker = "Motor Trend",
    date = as.Date("2026-07-21"),
    kicker_style = list(color = "#0054AD", size = "0.8em",
                        transform = "uppercase", spacing = "0.1em"),
    title_style = list(font = "Libre Franklin", weight = 800, size = "26px"),
    subtitle_style = list(color = "#666666", italic = TRUE),
    date_style = list(color = "#B8232F", weight = 600)
  )
}

}
\seealso{
\code{\link[=gt_538_caption]{gt_538_caption()}} for the footer equivalent.
}
