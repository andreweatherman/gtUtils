% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_booktabs.R
\name{gt_theme_booktabs}
\alias{gt_theme_booktabs}
\title{Academic booktabs theme for \code{gt} tables}
\usage{
gt_theme_booktabs(
  gt_object,
  accent = "#111111",
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{accent}{Character. A hex color for the three rules and the row-group
labels. Defaults to \code{"#111111"}, near-black, for the standard black rules.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
Three horizontal rules and nothing else, the way LaTeX booktabs draws them. A
heavy rule sits above the column labels, a lighter one below them, and a heavy
rule closes the body. There are no vertical rules, no rules between data rows,
and no fills. The type is a Times-compatible serif.
}
\details{
\code{accent} recolors the three rules and the row-group labels.
}
\section{Density}{


\code{density} sets the type and padding scale together. \code{"comfortable"} uses a
14px body with roomy rows, \code{"compact"} a 12px body with tight rows, and
\code{"social"} a 17px body with generous rows and a larger title, at the scale
\code{\link[=gt_save_crop]{gt_save_crop()}} and \code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_booktabs.png}{options: width=100\%}}
}

\examples{
\dontrun{
library(gt)

gt(head(mtcars[c("mpg", "hp", "wt")], 8)) \%>\% gt_theme_booktabs()

# a regression table, with significance stars and a colored rule
fit <- lm(mpg ~ wt + hp, data = mtcars)
data.frame(
  Term = names(coef(fit)),
  Estimate = coef(fit),
  p = summary(fit)$coefficients[, 4]
) \%>\%
  gt() \%>\%
  fmt_number(Estimate, decimals = 3) \%>\%
  gt_significance(Estimate, p) \%>\%
  gt_theme_booktabs(accent = "#1A3E6F")
}

}
\seealso{
\code{\link[=gt_theme_tufte]{gt_theme_tufte()}} for a lighter, minimal-ink relative, and
\code{\link[=gt_significance]{gt_significance()}} for significance notation.
}
