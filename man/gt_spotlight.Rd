% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_spotlight.R
\name{gt_spotlight}
\alias{gt_spotlight}
\title{Focus on rows of a \code{gt} table by dimming the rest}
\usage{
gt_spotlight(
  gt_object,
  rows,
  columns = gt::everything(),
  fill = NULL,
  text_color = NULL,
  bold = TRUE,
  accent_color = NULL,
  accent_width = 4,
  accent_column = NULL,
  dim_color = "#BBBBBB",
  if_none = c("warn", "dim", "ignore")
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{rows}{The rows to focus on. Either an expression evaluated against the
table's data, such as \code{cyl == 8}, or a numeric vector of row indices.}

\item{columns}{The columns the spotlight covers. Cells outside this selection
are dimmed, in the focused rows as well as the others, so only the selected
cells are left at full strength. Defaults to all of them, which lights the
whole row.}

\item{fill}{Optional. A hex color for the fill behind the focused rows.
Defaults to \code{NULL}.}

\item{text_color}{Optional. A hex color for the text of the focused rows.
Defaults to \code{NULL}.}

\item{bold}{Logical. Should the focused rows be bolded? Defaults to \code{TRUE}.}

\item{accent_color}{Optional. A color for a bar drawn on the left edge of the
focused rows. Supplying one is what turns the bar on, the same way \code{fill}
and \code{text_color} work. Defaults to \code{NULL}, no bar.}

\item{accent_width}{Numeric. The width of that bar in pixels, used only when
\code{accent_color} is given. Defaults to \code{4}.}

\item{accent_column}{The column to draw the bar against, on its left edge. If
\code{NULL}, the leftmost rendered column is used, which is what marks the start
of the row. Name a column when the row you are lighting sits elsewhere, as
with the second block of a \code{\link[=gt_snake]{gt_snake()}} table. More than one may be given,
which draws a bar against each. Defaults to \code{NULL}.}

\item{dim_color}{Character. The text color applied to every other row. Pass
\code{NULL} to leave the other rows alone, emphasizing without dimming. Defaults
to \code{"#BBBBBB"}.}

\item{if_none}{Character. What to do when \code{rows} matches nothing. \code{"warn"}
leaves the table alone and says so. \code{"dim"} dims the whole table, which is
what you want when several tables are laid out together with \code{\link[=gt_grid]{gt_grid()}} and
the highlighted row lives in only one of them. \code{"ignore"} leaves it alone
silently. Defaults to \code{"warn"}.}
}
\value{
Returns a modified \code{gt} table with the chosen rows emphasized.
}
\description{
Emphasizes one or more rows and mutes everything else.
\code{gtExtras::gt_highlight_rows()} makes a row louder; this leaves it as the only
row at full strength.
}
\details{
Rows can be given as a filtering expression evaluated against the underlying
data, or as plain row numbers.
}
\examples{
\dontrun{
library(gt)

cars <- head(mtcars[c("mpg", "cyl", "hp")], 8)
cars$model <- rownames(cars)

# by expression
gt(cars) \%>\% gt_spotlight(rows = cyl == 8)

# by position, with an accent bar
gt(cars) \%>\%
  gt_spotlight(rows = 2, accent_color = "#0054AD", fill = "#EEF3FA")

# narrowing the spotlight to some columns dims the rest of the row too
gt(cars) \%>\% gt_spotlight(mpg:cyl, rows = 3, accent_color = "darkblue")

# put the bar somewhere other than the first column
gt(cars) \%>\%
  gt_spotlight(hp, rows = 3, accent_color = "darkblue", accent_column = hp)

# across several tables: blocks without the row dim rather than staying lit
chunks <- split(cars, ceiling(seq_len(nrow(cars)) / 4))
tbls <- lapply(chunks, function(x) {
  gt(x) \%>\% gt_spotlight(rows = cyl == 8, if_none = "dim")
})
gt_grid(tbls, ncol = 2)
}

}
\seealso{
\code{\link[=gt_cutline]{gt_cutline()}} for marking a threshold rather than a row.
}
