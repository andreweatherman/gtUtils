% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_scale_note.R
\name{gt_scale_note}
\alias{gt_scale_note}
\title{Scale columns and disclose the scaling}
\usage{
gt_scale_note(
  gt_object,
  columns,
  divisor = 1000,
  note = NULL,
  where = c("source_note", "label", "both"),
  label_suffix = NULL,
  decimals = 0,
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The column or columns to scale.}

\item{divisor}{Numeric. The amount to divide by. Defaults to \code{1000}.}

\item{note}{Optional. The disclosure text. If \code{NULL}, it is derived from
\code{divisor}, giving "Figures in thousands." for \code{1e3}, "Figures in millions."
for \code{1e6}, and so on. Divisors without a common name fall back to
"Figures divided by 2,500." Defaults to \code{NULL}.}

\item{where}{Character. Where the disclosure goes. One of \code{"source_note"},
\code{"label"} to append a suffix to the column labels instead, or \code{"both"}.
Defaults to \code{"source_note"}.}

\item{label_suffix}{Optional. The suffix appended to column labels when \code{where}
includes \code{"label"}. If \code{NULL}, it is derived from \code{divisor} (for example,
\code{"(000s)"}). Defaults to \code{NULL}.}

\item{decimals}{Integer. The number of decimal places for the scaled values.
Defaults to \code{0}.}

\item{...}{Additional arguments passed to \code{gt::fmt_number}.}
}
\value{
Returns a modified \code{gt} table with the columns scaled and the scaling
disclosed.
}
\description{
Divides the selected columns and records the scaling in the same call, so the
numbers and the disclosure cannot drift apart.
}
\details{
Scaling is applied through \code{gt::fmt_number()} and its \code{scale_by} argument, so
the underlying data is left untouched.
}
\examples{
\dontrun{
library(gt)

revenue <- data.frame(
  Segment = c("Cloud", "Devices", "Services"),
  FY24 = c(4820000, 2110000, 1360000),
  FY23 = c(4100000, 2260000, 1180000)
)

# values render as 4.8, 2.1, 1.4 with "Figures in millions." beneath
gt(revenue) \%>\% gt_scale_note(c(FY24, FY23), divisor = 1e6, decimals = 1)

# disclose in the column labels instead
gt(revenue) \%>\% gt_scale_note(c(FY24, FY23), divisor = 1e3, where = "label")
}

}
