% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_outliers.R
\name{gt_outliers}
\alias{gt_outliers}
\title{Flag outlying values in a \code{gt} table}
\usage{
gt_outliers(
  gt_object,
  columns,
  method = c("iqr", "sd", "bounds"),
  threshold = NULL,
  bounds = NULL,
  side = c("both", "high", "low"),
  fill = NULL,
  color = NULL,
  bold = TRUE,
  symbol = NULL,
  note = NULL
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{columns}{The column or columns to test. Non-numeric columns are skipped.}

\item{method}{Character. How to decide what counts as an outlier. One of
\code{"iqr"}, \code{"sd"}, or \code{"bounds"}. Defaults to \code{"iqr"}.}

\item{threshold}{Numeric. The cutoff used by \code{"iqr"} and \code{"sd"}. If \code{NULL}, the
convention for the method is used: \code{1.5} for \code{"iqr"} and \code{3} for \code{"sd"}.
Defaults to \code{NULL}.}

\item{bounds}{A length-2 numeric vector giving \code{c(lower, upper)}, required when
\code{method} is \code{"bounds"}. Use \code{NA} for an open end, such as \code{c(NA, 100)}.
Defaults to \code{NULL}.}

\item{side}{Character. Which tail to flag. One of \code{"both"}, \code{"high"}, or
\code{"low"}. Defaults to \code{"both"}.}

\item{fill}{Optional. A hex color for the cell fill behind flagged values.
Defaults to \code{NULL}, which applies no fill.}

\item{color}{Optional. A hex color for flagged text. If \code{NULL}, a warning red
is used, swapped for a readable alternative when it would not have enough
contrast against \code{fill}. Defaults to \code{NULL}.}

\item{bold}{Logical. Should flagged values be bolded? Defaults to \code{TRUE}.}

\item{symbol}{Optional. A marker appended to flagged values, such as \code{"†"}.
Defaults to \code{NULL}.}

\item{note}{Optional. A source note describing the rule that was applied. Pass
\code{TRUE} for wording generated from \code{method} and \code{threshold}, a string for your
own, or \code{NULL} for no note. Defaults to \code{NULL}.}
}
\value{
Returns a modified \code{gt} table with outlying values marked.
}
\description{
Marks cells that fall outside a threshold, either beyond an interquartile
fence, beyond a number of standard deviations, or outside bounds you supply.
}
\details{
\code{gt::data_color()} shades a continuous scale; this flags a value as unusual.
Thresholds are computed separately for each column, so every column is judged
against its own distribution.

The default rule is the interquartile fence, not standard deviations, because
an SD fence is built from a spread that the outlier itself inflates. On
\code{c(10.2, 10.4, 10.1, 19.8, 10.3, 10.0)} the mean is 11.8 and the standard
deviation 3.92, so a three-SD fence reaches 23.6 and misses the 19.8. The
quartiles barely move, so the IQR fence stops at 10.75 and catches it. The
masking is worst in small samples. Use \code{method = "sd"} if you want it anyway.
}
\examples{
\dontrun{
library(gt)

assays <- data.frame(
  Sample = paste0("S", 1:6),
  Run1 = c(10.2, 10.4, 10.1, 19.8, 10.3, 10.0),
  Run2 = c(9.9, 10.1, 10.3, 10.2, 2.1, 10.4)
)

# the default fence catches both the high and the low reading
gt(assays) \%>\% gt_outliers(c(Run1, Run2), note = TRUE)

# an explicit acceptance range, flagging only the high side
gt(assays) \%>\%
  gt_outliers(c(Run1, Run2), method = "bounds", bounds = c(9, 11),
              side = "high", fill = "#FDECEA", symbol = "†")

# works on wider data too
gt(head(airquality, 12)) \%>\% gt_outliers(c(Ozone, Wind, Temp))
}

}
\seealso{
\code{\link[=gt_highlight_na]{gt_highlight_na()}} for missing values, and \code{\link[=gt_spotlight]{gt_spotlight()}} for
drawing attention to whole rows.
}
